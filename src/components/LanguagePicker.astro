---
import * as m from '../paraglide/messages';
import { availableLanguageTags, languageTag } from '../paraglide/runtime';

const makeUrlForLanguage = (lang: string) =>
  `/aiello-hotels/${lang}${Astro.url.pathname.replace(`/${languageTag()}`, '')}`;
---

<ul class="hidden md:flex flex-row items-center gap-4">
  {availableLanguageTags.map((lang) => (
    <li>
      <a class="text-base" href={makeUrlForLanguage(lang)}>{m.goToLanguage(undefined, { languageTag: lang })}</a>
    </li>
  ))}
</ul>